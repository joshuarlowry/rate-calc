import { defineConfig } from 'vite';

// For GitHub Pages, relative base works for both user and project pages.
// If you prefer explicit base, set to '/your-repo-name/'.
export default defineConfig({
  base: './',
});


